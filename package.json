{
  "name": "microfrontend-vite-crud-customer",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "run-s build serve",
    "build": "pnpm --parallel --filter=\"!./host\" build",
    "test": "pnpm --parallel --filter=\"!./host\" test",
    "test:ui": "pnpm --parallel --filter=\"!./host\" test:ui",
    "test:watch": "pnpm --parallel --filter=\"!./host\" test:watch",
    "serve": "run-p serve-remotes serve-host",
    "serve-remotes": "pnpm --parallel --filter=\"!./host\" preview",
    "serve-host": "pnpm --parallel --filter=\"./host\" dev",
    "stop": "kill-port --port 3000,3001"
  },
  "private": true,
  "devDependencies": {
    "@originjs/vite-plugin-federation": "^1.2.1",
    "kill-port": "^2.0.1",
    "npm-run-all": "^4.1.5"
  }
}
